import { marsaiColors } from "../../theme/marsai";

// Styles pour les champs de saisie dans le panneau
export const inputCls = "panel-input";

// Styles pour la surface du panneau
export const panelSurfaceStyle = {
  background: "var(--panel-surface-bg)",
  border: "1px solid var(--panel-surface-border)",
} as const;

// Styles pour les couleurs MarsAI
export { marsaiColors };
