export type Film = {
  id: string;
  title: string;
  synopsis?: string;
  year?: string;
  director?: string;
  youtubeId?: string;
  // TODO: ajouter les champs nécessaires (durée, genres, participants, etc.)
};
